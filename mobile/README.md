# Hubera Maps — app native

Expo 52 (même base que Fuel) · Android `ovh.delhomme.maps` · iOS `ovh.delhomme.maps`.

Voir le cahier : [`../docs/HUBERA-MAPS-CAHIER.md`](../docs/HUBERA-MAPS-CAHIER.md).

```bash
npm install
npx expo start
```

Deep link Fuel : `hubera-maps://track?tripId=1&mode=free`
